# 모의 계좌 견고화 — 포지션·잔고 정합 (개발계획서 v0.1)

## 원본 요구사항 (PM 발화 그대로 인용)
> 그냥 메모만 해놓고 모의 계좌를 더 견고하게 다듬자
> (견고화 축: **포지션·잔고 정합** — PM 선택 2026-06-16)

## 배경 (이번 세션 실증)
- 6/16 청산: KIS 잔고조회 실패(EGW00201) → `_sync_managed_positions_with_account` 정합 불가 →
  유령(auto_imported) 포지션 잔존 → EOD가 9종목 시장가 매도 → **전량 거부(tot_ccld_qty=0)** → CRITICAL.
- 정합 함수는 이미 존재하나 **KIS 잔고조회 성공이 전제**. 조회 실패 시 무방비.
- RPS 15→5+누진감속(커밋 5d8fbe4)이 조회 실패의 1차 원인을 완화 = 1차 방어.

## 구현 범위 (견고화 = 폴백 안전 + 청산 직전 정합)
- [ ] **T1 청산 직전 정합 선행**: EOD 청산 진입 시 KIS 잔고조회를 **재시도(짧은 backoff)** 후
  `_sync_managed_positions_with_account`로 position_manager를 실보유에 맞춤(유령 purge). 성공 시 실보유만 청산 대상.
- [ ] **T2 폴백 안전장치(핵심)**: KIS 잔고조회가 재시도 후에도 실패하면, **DB 포지션을 맹목적으로 시장가 매도하지 않는다.**
  - auto_imported(진입맥락 불명) 포지션은 **시장가 매도 금지** → 거부 폭주·CRITICAL 노이즈 제거.
  - 대신 "수동 확인 필요" 알림 1건으로 집약(현재처럼 9건 개별 실패 로그 X).
  - 최근 체결(`_has_recent_submitted_buy`/fills 검증)된 봇 포지션만 폴백 매도 허용.
- [ ] **T3 검증 로그**: 정합 결과(removed/added/qty_changed) 요약 1줄 + 폴백 사유 명시.

## 변경 파일 목록
| 파일 | 변경 | 이유 |
|------|------|------|
| `backend/services/engine/eod_liquidation.py` | 수정 | T1 재시도+정합 선행, T2 폴백 가드(auto_imported 시장가 금지) |
| `backend/services/engine/decision_engine.py` | 재사용 | `_sync_managed_positions_with_account` 호출(무수정 재사용) |
| `tests/unit/` 신규 | 추가 | 폴백 가드: 잔고조회 실패 시 auto_imported 미매도 단위테스트 |

## 요구사항 대조표
| 요구사항 | 반영 | 비고 |
|----------|------|------|
| 포지션·잔고 정합 견고화 | ✓ T1 | 청산 직전 실보유 동기화 |
| 유령 청산 실패·CRITICAL 근절 | ✓ T2 | 조회 실패 시 유령 시장가 매도 금지 |
| 회귀 안전 | ✓ | 조회 성공 시 동작 불변(기존 경로 유지) |

## 미결 확인 (PM)
1. **폴백 시 auto_imported 처리**: "시장가 매도 금지+알림"이 맞나? 아니면 (모의 한정) 그래도 1회 시도?
   - 권장: **금지+알림**(모의는 데이터 정합 우선, 거부 폭주가 더 해로움).
2. T1 재시도 횟수/간격(예: 2회 ×1.5s) — 기본값 제시, 이의 없으면 그대로.

## 완료 기준
- [ ] 잔고조회 성공 시: 청산 전 정합으로 유령 0, 실보유만 청산
- [ ] 잔고조회 실패 시: auto_imported 시장가 매도 0건, 알림 1건 집약
- [ ] 단위테스트(폴백 가드) 통과, 회귀 없음
- [ ] 다음 거래일 15:00 청산 로그로 검증
